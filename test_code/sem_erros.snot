/*Este codigo esta correto*/
class Classe {

	static int variavel1 = 0;
	const string nome = "essa é uma variável do tipo string.";

	Retorno funcaoComParametro(Classe parametro1, float parametro2, Lola parametro3) {
		Objeto obj;
		/*Comentario*/
		if( parametro1 == parametro2) {
			obj.chamaFuncao(variavel1);
		} else {
			obj.outraFuncao();
		}
	}

	void loop() {
		
		int array[3] = 0;
		for a in array {
			array[0] = variavel+1;
			if ( 3 > 5) {
				array[3] = funcao();	
			}
		}
	}

}

class Classe {}
